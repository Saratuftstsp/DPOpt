#!/bin/bash

# list of scales


# for each scale
#   preprocess
# for each query_name in tpch*.cpp
#   run each query file
#   redirect at least one party's output to a file names runtimes_${query_name}